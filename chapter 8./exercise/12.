/*
Q. Improve pront_until_s() from 8.5.2  . Test it. What makes a good set of test cases?
Give reasons. Then, write a print_until_ss() that prints until it sees a second occurrence of its quit argument.


2018.10.13
I still cannot understand what improve is.

*/



void print_until_ss(vector<string> v, string quit) {
	int i = 0;
	for (string s : v) {
		if (s == quit)++i;
		if (i == 2)return;
		cout << s << '\n';
	}
}
